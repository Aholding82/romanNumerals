# romanNumerals --> This is a module capable of converting any roman numeral into it's given integer
# the roman2.py file is a redo of the first roman numeral attempt, bby attempting to make the file a little cleaner
# and to use in built function practices rather than just brute forcing it